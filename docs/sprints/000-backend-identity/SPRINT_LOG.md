# 📋 Sprint Log: #000 — Backend / Identity (Legacy Onboarding)
**Sprint ID**: 000
**Stack / Layer**: backend / identity
**Date**: 2026-07-30
**Commit SHA at audit**: b27b5c2
**Status**: `CLOSED`

---

## 1. Purpose

Onboard an existing Django IAM codebase into `.agents` governance. Routed by `start_workflow.md` Phase 1.5 `first_run_scaffold` → `standardization_workflow.md` Phase 6 **Scenario C** (mature project, zero prior agentic traces). No code was modified in this sprint; it is an audit and documentation sprint only.

## 2. Scenario detection

| Signal | Observation | Conclusion |
| :--- | :--- | :--- |
| `docs/active_state.json` | Absent | First pipeline session in this host. |
| Source volume | 43 Python files, 2577 LOC across `backend/` | Substantial codebase. |
| Git history | 7 commits | Established, not greenfield. |
| Legacy agentic artifacts | No `task/`, `knowledge/`, `implementation_plan*.md`, `.agent_state/`, `.cursor/`, `.windsurfrules`, `copilot-instructions.md` | Nothing to absorb. |
| `CLAUDE.md` / `.claude/` | Present, but generated by `install_claude.sh` during this same session | This framework's own bridge, not prior interaction. |

→ **Scenario C.** Legacy Absorption Protocol (Scenario B) deliberately not executed.

## 3. Environment actions taken

| Action | Result |
| :--- | :--- |
| `pip_setup` | Created `.agents/venv_skillopt/`, installed `requirements-core.txt` (`graphifyy==0.8.30`), wrote `.agents/installed.lock`. |
| `read_graph` | Built `graphify-out/` — 3003 nodes, 3081 edges, 389 communities. Snapshot persisted at `graph_stats.json`. |
| `bridge_check` | Lock `4029c65` matched submodule HEAD; `.claude/{agents,commands,skills}` present. No reinstall needed. |
| `lightweight_sync` | `.agents` pinned at `v4.2.1`, the latest tag. No drift, no authorization requested. |

## 4. C4 Level 3 eligibility (computed, not assumed)

Declared `code_containers`: `[{"stack": "backend", "root": "backend/apps/"}]`.
Primitive filter: `.agents/scripts/denylists/python.txt` (31 entries).

| Container | Filtered nodes | Degree sum | Density |
| :--- | ---: | ---: | ---: |
| `users` | 194 | 560 | **2.89** |
| `core` | 27 | 48 | 1.78 |

The `backend` stack holds 2 containers (< 5), so the §2.1 safety floor applies: the single highest-density container qualifies → **`users`**. Per §2.1 rule 8 (bootstrap), this first run is **advisory, never blocking**.

## 5. Artifacts produced

| Artifact | Type |
| :--- | :--- |
| `docs/0_SYSTEM_OVERVIEW.md` | Documentation Entry Point (C4 L1-2) |
| `docs/active_state.json` | Zero Coordinate anchor |
| `docs/architecture/USERS_BLUEPRINT.md` | Reference (arc42-lite) |
| `docs/architecture/CORE_BLUEPRINT.md` | Reference (arc42-lite) |
| `docs/architecture/CONFIG_BLUEPRINT.md` | Reference (arc42-lite) |
| `docs/walkthroughs/USERS_WALKTHROUGH.md` | Historical narrative |
| `docs/walkthroughs/CORE_WALKTHROUGH.md` | Historical narrative |
| `docs/walkthroughs/CONFIG_WALKTHROUGH.md` | Historical narrative |
| `docs/roadmaps/GLOBAL_ROADMAP.md` | Future (P0/P1/P2 backlog) |
| `CHANGELOG.md` | Master Ledger, seeded at `[0.1.0]` |
| `docs/sprints/000-backend-identity/graph_stats.json` | Freshness-gate baseline |

## 6. Verified findings

Each was reproduced, not inferred:

| # | Finding | How verified |
| :--- | :--- | :--- |
| 1 | Test suite is uncollectable — `ImproperlyConfigured: Requested setting AUTH_USER_MODEL`. No `pytest-django` config exists. | `venv/bin/python -m pytest apps/users/tests.py apps/core/tests.py` |
| 2 | `HealthCheckView.get` passes `status_code=` to DRF `Response`, whose signature is `(data, status, template_name, headers, exception, content_type)`. `/health/` returns 500 unconditionally. | `inspect.signature(Response.__init__)` on the installed DRF |
| 3 | 79 `ruff` findings across `backend/`. | `venv/bin/ruff check backend/` |
| 4 | `ruff.toml` is the authoritative config; the entire `[tool.ruff]` block in `pyproject.toml` is dead. | `ruff check --show-settings` → `Settings path: ruff.toml` |
| 5 | Step-up auth is session-backed while DRF also accepts stateless JWT — JWT-only clients can never reach `/me/secrets/` or `/me/anonymize/`. | Read of `permissions.py:RequiresStepUp` vs `REST_FRAMEWORK.DEFAULT_AUTHENTICATION_CLASSES` |
| 6 | Registration OTP written as plaintext `OTP_PENDING:<code>` into `api_key_binance_encrypted`, bypassing `set_sensitive_data()`. | Read of `services.py:35-55` |
| 7 | `settings.py` is *not* a graph god-node; `apps/users/views.py` (degree 111) and `apps/users/managers.py` (109) lead. | Degree centrality computed from `graph.json` |

## 7. Governance notes

- **RA-14 applied**: an initial draft asserted `settings.py` was the highest-degree node. The graph disproved it; the claim was corrected in all three artifacts that carried it (`CONFIG_WALKTHROUGH.md` ×2, `CONFIG_BLUEPRINT.md` ×1) rather than only where first spotted.
- **Zero ADRs exist.** The inherited architecture has no recorded rationale. Each Blueprint §7 lists retroactive ADR candidates with their `documentation_standard.md §3.1` triggers.
- **No code was modified.** Remediation belongs to a subsequent sprint on `ai-sprint/[ID]` (RA-12), gated by an approved Implementation Plan.

---
*Closed under RA-05: Blueprints, Global Roadmap, Walkthroughs, and Master Ledger all updated.*
