# Development and test tooling. Installs the runtime set first, so a single
# `pip install -r requirements-dev.txt` gives a working checkout.
-r requirements.txt

pytest>=9.1.1
pytest-django>=4.12.0
pytest-cov>=7.1.0
factory-boy>=3.3.3
ruff>=0.16.0
